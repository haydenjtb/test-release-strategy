name: Full Playwright Suite Test

on:
  schedule:
    # runs every Thursday @ 4pm
    - cron: '0 14 * * 4'

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests
        run: echo "run just playwright"
